{
  "type": "array",
  "items": {
    "type": "object",
    "required": ["done", "content", "deadline"],
    "properties": {
      "done": { "type": "boolean" },
      "content": {
        "type": "string",
        "format": "task-content-format"
      },
      "deadline": {
        "type": "string",
        "format": "date"
      }
    }
  }
}
